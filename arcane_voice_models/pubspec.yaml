name: arcane_voice_models
description: Shared protocol models and codecs for the Arcane Voice client and proxy packages.
version: 1.0.0
repository: https://github.com/ArcaneArts/arcane_voice
environment:
  sdk: ^3.11.0
dependencies:
  artifact: ^1.0.34
dev_dependencies:
  artifact_gen: ^1.3.10
  build_runner: ^2.13.1
  lints: ^6.0.0
  test: ^1.25.6
scripts:
  build_runner: dart run build_runner build --delete-conflicting-outputs
